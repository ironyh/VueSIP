/**
 * Basic Audio Call Example - Main Entry Point
 *
 * This example demonstrates how to build a simple one-to-one audio call
 * application using VueSip composables.
 */

import { createApp } from 'vue'
import App from './App.vue'
import './style.css'

const app = createApp(App)
app.mount('#app')
