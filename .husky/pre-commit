#!/usr/bin/env sh
# Note: If this fails with "command not found", try:
# - Running `pnpm install` first
# - Using `git commit --no-verify` as a workaround
cd "$(dirname "$0")/.." && pnpm run lint-staged
