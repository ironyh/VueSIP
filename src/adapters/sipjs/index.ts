/**
 * SIP.js Adapter Module
 *
 * Exports the SIP.js adapter implementation for use with the adapter factory.
 *
 * Note: This is currently a stub implementation. Full SIP.js support requires:
 * 1. Installing sip.js: npm install sip.js
 * 2. Implementing the full adapter (see JsSipAdapter for reference)
 */

export { SipJsAdapter, isSipJsAvailable } from './SipJsAdapter'
export { SipJsCallSession } from './SipJsCallSession'
