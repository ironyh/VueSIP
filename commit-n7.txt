feat(Nivå 7): supervisor spy (ChanSpy) — Listen/Barge via AMI Originate

Adds supervisor live-listen and barge-in capability:

- AmiClient.originateSpy(): AMI Originate with Application: ChanSpy —
  calls the supervisor endpoint, connects to ChanSpy on the target
  channel. Mode 'listen' is silent (q flag); 'barge' whispers to both
  parties (B flag).
- SupervisorBoard: Listen + Barge buttons per agent row (visible only
  when the agent has an active call, agent.activeCallId is non-null).
- Runtime: handleSupervisorSpy wires the buttons to
  AmiClient.originateSpy() via the connected gateway tracked agent
  channel (from BridgeEnter events).

PBX capability proven: AMI accepts ChanSpy Originate commands with
Originate successfully queued (verified against live Asterisk 22.5.2).
Full two-browser spy audio test is a future item (requires a supervisor
browser instance registered on nurse_1002).
