name = "vuesiplay"
compatibility_date = "2025-12-10"
workers_dev = true

# Build the playground before deployment
# Install pnpm globally then run the build
[build]
command = "npm install -g pnpm && pnpm install --frozen-lockfile && pnpm run build:playground"

[assets]
directory = "dist-playground"
